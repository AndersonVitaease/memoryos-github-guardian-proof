{
  "name": "memoryos-github-guardian-proof",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "GH-00 - EXPERIMENTAL private proof lab. Single purpose: governed GitHub Pull Request merge.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
